userName: ${BROWSERSTACK_USERNAME}
accessKey: ${BROWSERSTACK_ACCESS_KEY}
framework: cucumber

platforms:
  - os: Windows
    osVersion: 11
    browserName: Chrome
    browserVersion: latest

projectName: Scotia Automation
buildName: Build Marketplace
buildIdentifier: '#${BUILD_NUMBER}'
parallelsPerPlatform: 1
debug: true
networkLogs: true